void main() {
  var name = 'Dart Programming';
  name = 'Flutter';
  // Optional types ( var ) ထဲမှာ ပထမဆုံးထည့်တဲ့  datatype ကိုပဲနောက်ထပ်ထည့်ထည်ံလို့ရမှာဖြစ်ပါတယ်/
  // ပထမဆံုး String type ထည့် ပြီးသားမို့ အခြား type တွေထည့်လို့မရတော့ပါဘူး
  // (error ပြပါလိမ့်မယ်) int = 4;
}
